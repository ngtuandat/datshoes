export interface ChildrenProps {
    children?: React.ReactNode;
}

export interface MessValidatorRegis {
    email: string;
    firstName: string;
    lastName: string;
    userName: string;
    password: string;
    password2: string;
}